import { StyleSheet } from "react-native"

export const CartStyles = StyleSheet.create({
    wrapper: {
        display: "flex",
        alignItems: "center",
        flex: 1,
        position: 'relative'
    }    
})