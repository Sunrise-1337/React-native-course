import { StyleSheet } from "react-native"

export const CustomTouchableStyle = StyleSheet.create({
    pressedOpacity: {
        opacity: 0.8,
    },
    
    unpressedOpacity: {
        opacity: 1,
    },

    overflowHidden: {
        overflow: 'hidden',
    },
});